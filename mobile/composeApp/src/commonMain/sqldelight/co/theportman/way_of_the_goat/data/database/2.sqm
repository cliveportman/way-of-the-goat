-- Migration 2: Remove profile_history table
-- Profile history is no longer used; empty past days show "No profile selected" instead

DROP INDEX IF EXISTS idx_profile_history_date;
DROP TABLE IF EXISTS profile_history;
